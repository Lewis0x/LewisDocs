# Codex CLI

Use this page to run **commands** safely.

Sample flow:

1. Open the terminal
2. Start `codex`

- Read the guide
- Test with line break<br>continuation

## Command examples

Run a small script:

```python
def hello(name):
    return f"Hello, {name}"
```

| Command | Purpose |
| --- | --- |
| `status` | report state |
| `login` | authenticate |

> Prefer *reviewed* runs.

Resources: [docs](https://example.com/codex) and icon ![Codex icon](/assets/codex.png).
