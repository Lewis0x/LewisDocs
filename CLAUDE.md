# Project Rules for AI Assistants

This file is read automatically by Claude Code (and similar AI agents). If
you are an AI working on this repository, **follow these rules without
asking for permission**; they reflect explicit decisions by the maintainer.

---

## Hard Rule: Local-First

**Every change must follow this exact path:**

```
local working copy (D:\Work\LewisDocs\)
   → npm run build  (verify locally)
   → git add + git commit
   → git push origin main
   → GitHub Actions CI
   → Cloudflare Pages deploy
   → https://lewisdocs.pages.dev/
```

**NEVER:**
- ❌ Edit on GitHub Web UI (`Edit this file` button) — bypasses local verification
- ❌ Modify Cloudflare Pages dashboard (Settings › Headers / Variables / Functions) — out-of-sync with git
- ❌ Touch `docs/.vitepress/dist/` directly — overwritten on next build
- ❌ Touch `docs/index.md` / `docs/theory.md` / `docs/comparison.md` / `docs/platforms/*.md` — these are *derived* from `source/` by `npm run prepare-content`; manual edits are wiped
- ❌ Suggest the user click around in any web UI for changes that can be expressed as files in this repo

When working in `D:\Work\LewisDocs\`, you always have direct `git push` to
`origin main` (which is `https://github.com/Lewis0x/LewisDocs.git`). No
`/tmp` clone, no manual sync.

---

## Annotation Tiers (when to add term annotations to prose)

Site is read by **managers and decision-makers**, not only same-domain CAD architects.
Annotate jargon that a CTO would need explained — but don't over-annotate. Decision tree:

```
Is it a brand/product name (Cloudflare Pages, Mermaid, VitePress)? → T4, skip
Is it CAD-specific (B-Rep, Parasolid, FeatureScript, TNP)?         → T1: glossary entry +
                                                                     <Term def="…"> or [link]
Is it a universal abbreviation (API, SDK, IDE, CI)?                → T3: glossary 缩写表 only,
                                                                     don't mark inline
Would a non-software-bg decision-maker need it explained?          → T2: inline
                                                                     <Term def="一句中文">
                                                                     (graduate to glossary at 3+ uses)
Otherwise                                                          → don't mark
```

**Density rules:**
- First mention per chapter only (not per page or paragraph)
- Skip code blocks, headings, table headers, `<sup>[百科 N]</sup>`, `[回链：...]`, mermaid nodes
- Max 2 annotations per paragraph

**Definition writing style:**
- 1-2 sentences, 30-80 Chinese chars, "**是什么 + 关键特征**"
- Don't reference other sections (avoid nested links in tooltips)
- Don't write English-only definitions

Run `python scripts/find_jargon.py` to discover unannotated jargon candidates.
Full methodology: `project-docs/05-annotation-methodology.md`.

---

## Source-of-Truth Map

| Type | Path | When you edit |
|---|---|---|
| Source content | `source/*.md` | Updating the 11 V4 research documents (incl. `术语表-V4.md` glossary) |
| Site config | `docs/.vitepress/config.ts` | Nav / sidebar / search / Mermaid / head meta |
| Theme | `docs/.vitepress/theme/` | Custom Vue components (`components/`), `custom.css`, `index.ts` |
| Public assets | `docs/public/` | `robots.txt`, `ai.txt`, `_headers`, `_honeypot/` |
| Pipeline | `scripts/*.py` | Import / rewrite-links / cite-link / watermark / audit logic |
| CI | `.github/workflows/*.yml` | Build + deploy pipeline (CF Pages active, GH Pages disabled) |
| GitLab fallback | `.gitlab-ci.yml` | Optional GitLab Pages mirror |
| Project meta | `project-docs/`, `README.md`, `LICENSE`, `CLAUDE.md` (this file) | Process / requirements / design / dev / usage / annotation / citation audit |
| **Derived (DO NOT EDIT)** | `docs/index.md`, `docs/theory.md`, `docs/comparison.md`, `docs/glossary.md`, `docs/platforms/*.md` | — generated by `npm run prepare-content` |
| **Derived (DO NOT EDIT)** | `docs/.vitepress/dist/`, `docs/.vitepress/cache/` | — generated by `npm run build` |

---

## Build pipeline (what each step does)

```
npm run prepare-content
   ├─ scripts/import_docs.py        source/*.md  →  docs/*.md (route-friendly names + frontmatter)
   ├─ scripts/rewrite_links.py      [回链：3.4 §三 …]  →  real markdown links (slugify must match VitePress)
   └─ scripts/link_citations.py     [百科 N] / [新闻 N] / [官方 N] / [第三方 N] / [书籍 N]  →  <a href="…">[百科 N]</a>
                                    (reads each doc's `## 参考来源` section to build the (kind,num)→URL map)

npm run build
   ├─ vitepress build docs           static site → docs/.vitepress/dist/
   └─ scripts/watermark.py           (1) strip VitePress generator meta tag
                                     (2) inject zero-width-char watermark per page,
                                         emit docs/.vitepress/dist/_watermark-manifest.json
                                     CI uploads manifest as 90-day artifact, then `rm -f`'s it
                                     before the CF Pages deploy — manifest is private.
```

Use `npm run build:no-watermark` for local debugging only; never commit/deploy without watermark.

### Auxiliary scripts (not in the build path)

| Script | Purpose |
|---|---|
| `scripts/find_jargon.py` | Scan `source/` for unannotated jargon → `jargon-report.md` (gitignored). Human decides T1–T4. |
| `scripts/audit_citations.py` | Extract every citation `[kind N]` from source docs → `audit_report.csv` / `.md` (gitignored). `--check-urls` does HTTP HEAD probes. Process tracked in `project-docs/06-citation-audit.md`. |
| `scripts/fix_term_quotes.py` | Fix inner ASCII `"` inside `<Term def="…">` (Vue HTML parser breaks otherwise — convert to U+201C/U+201D). |
| `scripts/scan_corpus.py` | Watermark recovery tool. Feed suspicious corpus + manifest → returns `(page_path, build_id)` of leak source. |

---

## Architecture in 30 seconds

- VitePress 1.5+ static site (with `vitepress-plugin-mermaid` for diagrams), source = 11 V4 Markdown research docs (~174k 字)
- Search = local index, Chinese tokenized via `Intl.Segmenter` (browser) / regex fallback (build-time Node)
- Hosted on **Cloudflare Pages** (project name: `lewisdocs`, URL: `https://lewisdocs.pages.dev/`)
- CI = **GitHub Actions** (`.github/workflows/cloudflare-pages.yml`); concurrency group `cloudflare-pages`
- Anti-crawler = 5 layers (robots.txt + meta noindex incl. AI bots + edge `_headers` + `_honeypot/` + zero-width watermark + CC BY-NC-ND 4.0 LICENSE w/ AI-use restriction) — see `project-docs/02-design.md` ADR-009
- Old GitHub Pages workflow is disabled (`if: false` in `pages.yml`); `.gitlab-ci.yml` is a parallel fallback target

---

## When in doubt

1. Read `project-docs/03-development.md` first — it has the full workflow, gotchas, debug tips
2. Read `project-docs/02-design.md` for the architectural rationale (8 ADRs)
3. Read `project-docs/05-annotation-methodology.md` before adding any `<Term>` / glossary entry
4. Read `project-docs/06-citation-audit.md` before touching `## 参考来源` sections in source docs
5. If the user asks for something that conflicts with these rules, surface
   the conflict explicitly before acting (don't silently violate rules)

---

## Common pitfalls (already learned the hard way)

- **VitePress search index is empty** → check `markdown.anchor.permalink` is not `false` (the indexer needs `<a href="#…">` inside headings to split sections)
- **First page load shows blank content** → watermark must be injected outside `#app` Vue mount (currently before `</body>`, in `scripts/watermark.py`)
- **Cloudflare project not found** → CF Pages project names must be lowercase (we use `lewisdocs` even though the repo is `LewisDocs`)
- **Search shows no results for "autocad"** → see "VitePress search index is empty" above; the bug is permalink-related, not the tokenizer
- **`.vitepress/` placement** → must be inside `docs/`, not at repo root (VitePress treats `docs/` as project root since `vitepress build docs`)
- **Slugify mismatch** → if anchors break, compare `scripts/rewrite_links.py` `vitepress_slugify` against `node_modules/vitepress/dist/node/chunk-*.js` — they must produce identical output
- **Vue scoped CSS + `:global(body|html …)` = entire page hidden** → never write `:global(body.X) .scoped-Y { ... }` in a `<style scoped>`; Vue 3.5 drops the scoped child and produces just `body.X { display: none }`, blanking the page for any user with that body class. **Put body/html-targeting rules in `docs/.vitepress/theme/custom.css` (unscoped).** Full postmortem: [project-docs/03-development.md §12.1](./project-docs/03-development.md)
- **`<Term def="...">` def value with inner ASCII `"` breaks Vue parser** → "Invalid end tag" build error. Use fullwidth `""` (U+201C/U+201D) inside def values, or run `python scripts/fix_term_quotes.py` to auto-convert
- **Wide table overflows under right outline** → flex item default `min-width: auto` (= min-content) won't shrink for wide content. Add `min-width: 0` on the flex item AND wrap tables in a `.table-scroll-wrapper` div (markdown.config does this automatically)
- **Lightbox shows blank modal for SVG** → `v-html` cannot render SVG inside an HTML container (namespace mismatch). Use `cloneNode(true) + appendChild` instead
- **Real-time UI bugs only show for "old" users with cached state in localStorage** → After deploying any change to OutlineToggle / OutlineResizer / Lightbox / Term, **must test with a browser that has prior localStorage state**, not just a fresh tab. SSR HTML being correct ≠ page renders correctly post-hydration
- **CSS `position: sticky` cannot anchor to page viewport when any ancestor has `overflow-x/y: auto`** → the sticky element anchors to the nearest scroll container, NOT the viewport. If `.table-scroll-wrapper` has `overflow-x: auto`, then `thead { position: sticky; top: 64px }` inside it sticks at "wrapper-top + 64", not "viewport-top + 64" — causing visual chaos when the page scrolls. Don't combine wrapper-with-overflow + sticky thead in pure CSS; need JS-driven cloned-thead approach.
- **Layout / sticky / scroll-related CSS PRs need actual browser verification** → bundle-rule-check + HTML-structure-check + clean-build are NECESSARY but NOT SUFFICIENT. Must open the live page, scroll, and look. (Took me PRs #10/#13/#16 across 2 days to learn this.)
- **`_watermark-manifest.json` leaked into deploy** → CI must `rm -f docs/.vitepress/dist/_watermark-manifest.json` *after* artifact upload and *before* `pages deploy`. Manifest is the only thing that maps watermark→page; if it leaks, watermark is useless
- **CF Pages deploy fails on first run** → the `Ensure project exists` step uses `pages project create lewisdocs --production-branch=main` with `continue-on-error: true`; project name **must** be lowercase
- **CF Pages deploy fails with `Invalid commit message, must be valid UTF-8` (code 8000111)** → emoji or 4-byte UTF-8 chars in `git log -1 --format=%B` body get forwarded via HTTP header (ASCII-only per RFC 7230). Files upload fine, deployment record creation rejects. **Keep commit message body ASCII-only**; reserve emoji for PR descriptions or README. Postmortem: [project-docs/03-development.md §12.8](./project-docs/03-development.md)

For full postmortems with root cause analysis, see [project-docs/03-development.md §12](./project-docs/03-development.md) — every entry there cost real downtime.
