# Synthetic handbook

Use `codex --help` before this fenced example:

```bash
echo "synthetic"
```

Open [guide](https://example.test/docs) and ![icon](./assets/agent.svg).
Visit https://example.test/reference
Run npm run ai:sync with --dry-run.
Keep MOONSHOT_API_KEY, agent_config, AGENTS.md, and ./configs/prod.yaml.
Products: Claude Code, Codex CLI, Moonshot API, kimi-k3.

> quoted note

- list item

| key | value |
| --- | --- |
| plain prose | sample |
